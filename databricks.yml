# This is a Databricks asset bundle definition for practice_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: practice_dab
  uuid: 4ec68abc-5f98-4d1b-918a-4b73c131916e

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-0034aaf4-0285.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-0034aaf4-0285.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/pathakrajat27@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/pathakrajat27@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: pathakrajat27@gmail.com
        level: CAN_MANAGE
